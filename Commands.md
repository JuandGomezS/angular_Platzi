# Angular Cheat Sheet

### -Install Angular CLI
```
npm i -g @angular/cli
```
### -Create angular app

```
ng new <ApplicationName>
```

### -Init angular app
```
ng serve optional: -o (auto open in browser) --port=XXXX (Set port to init app)
```
